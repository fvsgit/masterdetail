_schema-version: "2.0.0"
ID: "sapcrmbtxsapsupportcenter"
version: 1.0.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "sapcrmbtxsapsupportcenter"
    type: html5
    path: .
    parameters:
       version: ${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist
